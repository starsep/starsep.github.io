[project]
name = "starsep-github-io"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mdformat>=1.0.0",
    "mdformat-front-matters>=2.0.0",
]
